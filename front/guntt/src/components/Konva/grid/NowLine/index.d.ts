import React from "react";
interface NowLineProps {
    rowHeight: number;
    stageHeight: number;
    columnWidth: number;
}
declare const NowLine: ({ rowHeight, stageHeight, columnWidth }: NowLineProps) => React.JSX.Element;
export default NowLine;
